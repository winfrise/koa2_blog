const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database: {
    DATABASE: 'le_blog',
    USERNAME: 'fa-root',
    PASSWORD: 'root1234',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config